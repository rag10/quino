Acciones correctivas requeridas:

- Ahora mismo no se pueden seleccionar bar o body en el canvas. Arregla esto par que se puedan seleccionar.
- Cuando se pulsa la tecla esc. se debe de deselccionar lo que haya seleccionado.
- Hay un error con los markers cuando el elemento que los contiene está linkado. A la hora de moverlos, se desplazan otros markers que no tienen por qué hacerlo. Lo único que hay que chequear a la hoar de mover un marker es si hay alguna junta que lo esté enlazando con otro marker, ground o slider. En ese caso se debe mover el marker seleccionado, la junta y el otro marker/ground/slider. Esto aplica también si se modifica la posición de un marker desde el inspector. 
- Cuando se desplza un marker.
- En el inspector, los campos a editar true/false, com el de visibilidad deberán hacerse por medio de un desplegable.

----------------------------------------------

Acciones correctivas requeridas:

Al crear bar, si se clica en un marker existente, debe aparecer un diálogo para crear una junta. Este diálogo aparece, pero falta incluir el nombre de la junta. Además, luego se produce un bug en el que no se permite completar la creación de la barra, tanto si es el primer como el segundo marker. Al crear el body parece que este bug no ocurre, pero revisalo también.

También hay que corregir la forma de unir un marker con un slider.  

------------------------------------------------

Vamos a corregir varias cosas que creo que están relacionadas.
Tras simular, al volver a editar el modelo arrastrando markers en el canvas, se producen errores.
Para empezar, si no hay simulación, desactiva los botones de play,pause, etc.
Una vez haya simulación, si se intenta editar el modelo aparecerá un warning que indicará que si se modifica algo del modelo, la simulación se eliminará. Esto evitará errores.
Revisa todo esto con criterio. Si tienes cualqueir duda, vete preguntándome.


-------------------------------------------------

Vamos a añadir sensores al software. Los sensores nos van a ayudar a medir por el momento posiciones (lineales y angulares). Por el momento vamos a implmentar:
- Sensor puntual: en un marker, qeu va a registrar posiciónes, velocidades y aceleraciones x,y y absoluta.
- Sensor de distancia: entre dos markers, va a registrar la distancia entre ellos, así como la velocidad y aceleración relativa.
Sensor de ángulo: va a registrar el ángulo enter dos pares de markers, así como la velocidad angular y aceleración angular.

Esas señales se podrán utilizar a continuación para ser ploteadas en un viewer, pero para esto, espera a que te de detalles.

Vamso a crear un plan de implementación y vamos a hacerlo despacio, quiero ser yo quien guie las decisiones, dame respuestas cortas y hazme preguntas, propón ideas, pero no las ejecutes hasta que nos las consesues conmigo. No quiero respuestas largas, la forma de trabajar tiene que ser como si estuvieramos teniendo una conversación.

------------------------------------------------

Con respecto al viewer, quiero que sea una pieza separada para que no solo se pueda usar con la app GUI si no que se pueda llamar desde un un script (sin GUI) simplemente para plotear resultados, manipularlos, etc. Tengo un proyecto antiguo para esto. Estáa muy mal ejecutado, pero la idea y las funcionalidades se pueden extraer. Quiero que lo actualicemos y lo integremos. 
El viewer está basado en pyqtgraph, esto quiero mantenerlo, así como las utilidades de seleccionar los canales a plotear y si ponerlos en el eje x o y, mostrar el min y máximo, etc. Tracemos un plan para modificarlo de tal form que encaje de la mejor manera posible dentro de QUINO. Propón ideas, pero no las ejecutes hasta que nos las consesues conmigo.

------------------------------------------------

A la hora de crear juntas, unir markers con ground o sliders, etc. es necesario que no solo se puedan seleccionar en el canvas, si no que también e indistintamente se puedan seleccioanar en el model tree.

Vamos a modernizar el model tree, añade iconos visuales y mejoras en cuanto a la experiencia de usuario.

Hay un tema que corregir también: las rótulas solo unen dos markers, pero un mismo marker puede estar unido a dos rótulas y por tanto a dos markers distintos. Esto puede producir errores, ya que tal como está implementado ahora el desplazamiento de markers en el canvas, a veces se mueven solo dos rótulas, pero las otra no, etc. Hay que hacer más robusto esto y buscar todos los markers que estén conectados en serie por medio de juntas para meverlos todos a la vez. Ejemplo: junta B1.M1-B2.M1 y junta B2.M1-B3.M1. Si muevo B3.M1 ahora se mueve solo B2.M1, pero no B1.M1. Corrije la función para que busque en cadena todos los markers conectados en el mismo punto espacial por medio de jutnas y los mueva a la vez. Recuerda que el resto de markers del modelo no se deben modificar.

Ahora mismo cuando se usa "connect marker to slider" el marker no se actualiza a la posición del slider o viceversa y hay un gap espacial. corrige eso tambén.

Separa los botones de New Graph y Trajectories de los de simulación en el bloque de Results.

Cuando se selecciona un botón para alguna función que después requiere alguna acción sobre el canvas, deben activarse. Hay algunos que lo hacen como crear bar o body, gorund joint, etc., pero hay otros que no, como los de crear sensores o drivers. 
Cuando se presiona la tecla ESC debe volver a la función seleccionar.

En el inspector, cuando se seleccione un bar o body quiero que se indiquen los markers que lo forman y si se hace doble clic sobre el marker, se selecciona el marker.
En el inspector, cuando se seleccione un marker, quiero que se indique el bar o body al que esté unido, las juntas a las que esté unido inidando el tipo de junta y el marker con el que la une (o ground o slider) y si se hace doble clic sobre alguno de ellos se seleccione.
En el inspector, cuando se seleccione una junta, quiero que se indique los marker que una indicando el bar/body al que pertenecen y si se hace doble clic sobre alguno de ellos se seleccione.
Lo mismo con los drivers y lo sensores.

------------------------------------------------

A continuación vamos a crear un plan para implementar la función de sketch. Vvamos a hacerlo despacio, quiero ser yo quien guie las decisiones, dame respuestas cortas y hazme preguntas, propón ideas, pero no las ejecutes hasta que nos las consesues conmigo. No quiero respuestas largas, la forma de trabajar tiene que ser como si estuvieramos teniendo una conversación. Para empezar lee docs/Specification y docs/spec_part002.md, ya que ahí está bastante bien indicado. 

Lo primero vamso a hacer un commit y push y crear una nueva rama para poder volver atrás en caso de romper algo.


------------------------------

Cuando se esté en modo sketch, no se podrán seleccionar los elementos del modelo, que quedarán en un segundo plano. Cuando se seleccione el modo modelo, será al contrario, los elementos del sketch no se podrán seleccionar. ¿Cómo podríamos implemetnarlo?


