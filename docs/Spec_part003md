Acciones correctivas requeridas:

- Ahora mismo no se pueden seleccionar bar o body en el canvas. Arregla esto par que se puedan seleccionar.
- Cuando se pulsa la tecla esc. se debe de deselccionar lo que haya seleccionado.
- Hay un error con los markers cuando el elemento que los contiene está linkado. A la hora de moverlos, se desplazan otros markers que no tienen por qué hacerlo. Lo único que hay que chequear a la hoar de mover un marker es si hay alguna junta que lo esté enlazando con otro marker, ground o slider. En ese caso se debe mover el marker seleccionado, la junta y el otro marker/ground/slider. Esto aplica también si se modifica la posición de un marker desde el inspector. 
- Cuando se desplza un marker.
- En el inspector, los campos a editar true/false, com el de visibilidad deberán hacerse por medio de un desplegable.

----------------------------------------------

Acciones correctivas requeridas:

Al crear bar, si se clica en un marker existente, debe aparecer un diálogo para crear una junta. Este diálogo aparece, pero falta incluir el nombre de la junta. Además, luego se produce un bug en el que no se permite completar la creación de la barra, tanto si es el primer como el segundo marker. Al crear el body parece que este bug no ocurre, pero revisalo también.

También hay que corregir la forma de unir un marker con un slider.  

------------------------------------------------

Vamos a corregir varias cosas que creo que están relacionadas.
Tras simular, al volver a editar el modelo arrastrando markers en el canvas, se producen errores.
Para empezar, si no hay simulación, desactiva los botones de play,pause, etc.
Una vez haya simulación, si se intenta editar el modelo aparecerá un warning que indicará que si se modifica algo del modelo, la simulación se eliminará. Esto evitará errores.
Revisa todo esto con criterio. Si tienes cualqueir duda, vete preguntándome.


-------------------------------------------------

Vamos a añadir sensores al software. Los sensores nos van a ayudar a medir por el momento posiciones (lineales y angulares). Por el momento vamos a implmentar:
- Sensor puntual: en un marker, qeu va a registrar posiciónes, velocidades y aceleraciones x,y y absoluta.
- Sensor de distancia: entre dos markers, va a registrar la distancia entre ellos, así como la velocidad y aceleración relativa.
Sensor de ángulo: va a registrar el ángulo enter dos pares de markers, así como la velocidad angular y aceleración angular.

Esas señales se podrán utilizar a continuación para ser ploteadas en un viewer, pero para esto, espera a que te de detalles.

Vamso a crear un plan de implementación y vamos a hacerlo despacio, quiero ser yo quien guie las decisiones, dame respuestas cortas y hazme preguntas, propón ideas, pero no las ejecutes hasta que nos las consesues conmigo. No quiero respuestas largas, la forma de trabajar tiene que ser como si estuvieramos teniendo una conversación.

------------------------------------------------

Con respecto al viewer, quiero que sea una pieza separada para que no solo se pueda usar con la app GUI si no que se pueda llamar desde un un script (sin GUI) simplemente para plotear resultados, manipularlos, etc. Tengo un proyecto antiguo para esto. Estáa muy mal ejecutado, pero la idea y las funcionalidades se pueden extraer. Quiero que lo actualicemos y lo integremos. 
El viewer está basado en pyqtgraph, esto quiero mantenerlo, así como las utilidades de seleccionar los canales a plotear y si ponerlos en el eje x o y, mostrar el min y máximo, etc. Tracemos un plan para modificarlo de tal form que encaje de la mejor manera posible dentro de QUINO. Propón ideas, pero no las ejecutes hasta que nos las consesues conmigo.

------------------------------------------------

A la hora de crear juntas, unir markers con ground o sliders, etc. es necesario que no solo se puedan seleccionar en el canvas, si no que también e indistintamente se puedan seleccioanar en el model tree.

Vamos a modernizar el model tree, añade iconos visuales y mejoras en cuanto a la experiencia de usuario.

Hay un tema que corregir también: las rótulas solo unen dos markers, pero un mismo marker puede estar unido a dos rótulas y por tanto a dos markers distintos. Esto puede producir errores, ya que tal como está implementado ahora el desplazamiento de markers en el canvas, a veces se mueven solo dos rótulas, pero las otra no, etc. Hay que hacer más robusto esto y buscar todos los markers que estén conectados en serie por medio de juntas para meverlos todos a la vez. Ejemplo: junta B1.M1-B2.M1 y junta B2.M1-B3.M1. Si muevo B3.M1 ahora se mueve solo B2.M1, pero no B1.M1. Corrije la función para que busque en cadena todos los markers conectados en el mismo punto espacial por medio de jutnas y los mueva a la vez. Recuerda que el resto de markers del modelo no se deben modificar.

Ahora mismo cuando se usa "connect marker to slider" el marker no se actualiza a la posición del slider o viceversa y hay un gap espacial. corrige eso tambén.

Separa los botones de New Graph y Trajectories de los de simulación en el bloque de Results.

Cuando se selecciona un botón para alguna función que después requiere alguna acción sobre el canvas, deben activarse. Hay algunos que lo hacen como crear bar o body, gorund joint, etc., pero hay otros que no, como los de crear sensores o drivers. 
Cuando se presiona la tecla ESC debe volver a la función seleccionar.

En el inspector, cuando se seleccione un bar o body quiero que se indiquen los markers que lo forman y si se hace doble clic sobre el marker, se selecciona el marker.
En el inspector, cuando se seleccione un marker, quiero que se indique el bar o body al que esté unido, las juntas a las que esté unido inidando el tipo de junta y el marker con el que la une (o ground o slider) y si se hace doble clic sobre alguno de ellos se seleccione.
En el inspector, cuando se seleccione una junta, quiero que se indique los marker que una indicando el bar/body al que pertenecen y si se hace doble clic sobre alguno de ellos se seleccione.
Lo mismo con los drivers y lo sensores.

------------------------------------------------

A continuación vamos a crear un plan para implementar la función de sketch. Vvamos a hacerlo despacio, quiero ser yo quien guie las decisiones, dame respuestas cortas y hazme preguntas, propón ideas, pero no las ejecutes hasta que nos las consesues conmigo. No quiero respuestas largas, la forma de trabajar tiene que ser como si estuvieramos teniendo una conversación. Para empezar lee docs/Specification y docs/spec_part002.md, ya que ahí está bastante bien indicado. 

Lo primero vamso a hacer un commit y push y crear una nueva rama para poder volver atrás en caso de romper algo.


------------------------------

Cuando se esté en modo sketch, no se podrán seleccionar los elementos del modelo, que quedarán en un segundo plano. Cuando se seleccione el modo modelo, será al contrario, los elementos del sketch no se podrán seleccionar. ¿Cómo podríamos implemetnarlo?

--------------------------------------


Para distinguirlos visualmente, Los iconos de sketch los vamos a poner en tonos grises. Los botones de cinemática los vamos a poner en tonos azules y los de dinámca en tonos anaranjados.

Vamso a revisar lso nombres en los botones para qeu nos desborden. Más cortos, por ejemplo sketch line pondríamos solo Line y vamos a redistribuir los botones en grupos más consistentes.

------------------------------------------------

Revisa la implementación de las contraints para adecuarlas mejor a un software profesional tipo autocad. Ahoar mismo por ejemplo, la constraint de coincidencia no permite seleccionar markrs, si no que selecciona segmentos, y lo que hace es llevarlos a un mismo punto. Esto no tien mucho sentido, tiene más sentido poder seleccionar puntos, normalmente de dos lineas distintas. En el caso de distancia, deberíamos ampliar la fucnionalidad a no solo segmentos si no, poder selecionar dos puntos, un segmento y un punto, dos segmentos paralelos, etc.
En la implemetnación del arco, deben aparecer dos puntos al final del arco (como si fuera un segmento curvado) para poder editarlos. Revisa ambas implementaciones, ya que no funcionan bien. La restricción de distancia debería fucnionar también en circunferecnias para añadir radios. Cuando se crea una circunferencia el segundo punto no tiene mucho sentido. Hay que extender la fucnión de simetría en la linea de lo que estamos hablando. Los DOF deberían calcularse para cada elemento teniendo en cuenta las restricciones no solo de ese elemento si no de los elementos a los que está unido por otras restricciones y mostrarse en verde si 0 DoF para ese elemento no se si se hace así internamente, pero a veces se muestra el sketch en rojo cuando no debería ser así, y de repente arrastras un punto y el solver converge. No pongas una linea discontinua al añadir las restricciones de perpendicularidad o paralelismo, pon el símbolo en ambos segmentos.


* Gravity por defecto OFF.
* Cuando se cambia los parámetros de simulación (tiempo de simulación, numero de frames o delta t), debería anularse la simulación anterior al igual que cuando se cambia algo del modelo.


* El centro de masas de una Punctual Mass no se debe poder desplazar del único marker que la forma. 
* El centro de masas en las barras, debe contenerse en el segmento que une los dos puntos. En vez de definirlo por coordenadas, vamos a definirlo como un porcentaje de la longitud de la barra o como una longitud partiendo del primer marker y como máximo la longitud de la barra (ambas opciones deben poderse editar, cambiando la otra en consecuencia). Si la posición de uno de los markers se modifica, el CoM cambiará en consecuencia manteniendo la distancia relativa y proporcional a los markers.

-----------------------------------------------------------------

* Implementar la posibilidad de añadir fricciones a las rótulas. Por defecto se incluirán rótulas y sliders sin fricción y en el inspector se podrá editar. Se debe considerar la posibilidad de incluir fricción de coulomb y/o viscosa.
* Para los selectores de frames: los botones de incremento o reducción deben cambiar en un orden de magnitud inferior al valor de forma que si el valor es 1000, los botones deben cambiar en +/-100. Si el valor es 54608, os botones deben cambiar en +/- 5460
* Para el selector Delta t y speed: deben cambiar de la siguiente forma: si el valor es 0.001, los botones deben cambiar +/-0.0005. Si el valor es 0.003, los botones deben cambiar +/-0.0005. Si el valor es es 0.07, los botones deben cambiar +/-0.005. Si el valor es 2.5, los botones deben cambiar +/-0.5.
* Las cargas deberán poder definirse como función de variables como el tiempo o señales medidas por los sensores. Por ejemplo, en función de una distancia medida entre 2 markers (similar a un muelle), o en función de un ángulo.
* Estudiar si podemos incluir un sistema de coordenada local para los elementos. 
	• Particularidades de cada elemento: 
		• Punctual Mass no tendrá origen de coordenadas.
		• Bar tendrá origen de coordenadas con x alineado siempre con la dirección del segmento. Los markers que definen la barra y el CoM siempre estarán en el eje local x.
		• Body tendrá el sistema un coordenadas que se definirá con respecto al sistema absoluto de coordenadas por desplazamiento y rotación.
	• El origen de coordenadas local no tiene por qué estar en un marker, aunque por defecto se creará en el primer marker que se cree.
	• En el inspector tendríamos un seleccionador para poder editar la posición de los markers con respecto al sistema de coordenadas global o local.
	• El sistema de coordenadas local se debe poder mostrar u ocultar en el canvas. En el model tree deberá aparecer como hijo del elemento y se podrá editar desde el inspector. 
* Correcciones en sketch a nivel GUI: 
	• Añadir por defecto un punto O en el origen y fijo .
	• Cambiar el icono de rectángulo para que sea representativo (ahora es igual que el de línea).
	• El círculo debería poder cambiar el diámetro al arrastrarlo sin cambiar el centro, teniendo en cuenta por supuesto las restricciones que le apliquen.
	• Arco por tres puntos contenidos en el arco lo vamos a eliminar. Solo vamos a dejar la opción de definirlo con el primer marker indicando el centro y los otros dos el inicio y el final del arco. Revísalo bien para que los markers 2 y 3 coincidan con el principio y el final del arco.
	• Restricción de distancia: 
		• Al definirla la distancia entre dos markers, la cota aparece a una distancia fija y siempre la misma. Vamos a mejorar esto poniendo la cota a la distancia que el usiario clique la tercera vez con respecto a los dos primeros markers seleccionados. Para modificar esa distancia desde los markers donde se aplica la restricción hasta donde se muestra la cota, se deberá poder hacer desde el inspector.
		• La restricción de distancia también se podrá definir sobre una linea (no solo sobre 2 markers).
		• Al definir la restricción de distancia en una circunferencia para definir un radio, no se muestra la cota en el canvas, incluye flecha y el valor para mostrarla. Aunque en la GUI se utilice un solo botón para restricciones de distancia entre 2 markers o para definir un radio, a la hora de crearlo deberán ser tipos distintos "distance" / "radius". 
		• Vamos a añadir dos botones y funciones adicionales para añadir restricciones de distancia vertical y horizontal para aplicar la restricción de distancia en la proyección sobre el eje x o y. En este caso solo se podrán seleccionar markers o líneas, no circunferencias.
		• Se debe poder seleccionar las cotas de distancias y radio directamente sobre el valor o la cota en el canvas para poder modificarla en el inspector. Con doble clic se abrirá una pop-up para modificarlo.
	• Al poner una restricción de ángulo, da un error de "Expected angle but got unitless", pero el campo es un spin box que no deja introducir unidades. Arréglalo.
	• Las restricciones de paralelismo, perpendicularidad y equal se deben poder seleccionar desde su símbolo en el canvas. Para una restricción debe aparecer el símbolo en las dos líneas que quedan relacionadas. Eliminar la linea discontinua que indica la restricción para estos casos.
	• En el caso de tangencia, se debe crear un punto adicional en el punto de tangencia entre la línea y la circunferencia. También se debe permitir la tangencia entre dos circunferencias y con arcos.
	• Los elementos que sean construction=True, solo se mostrarán en modo Sketch. En modo Model o Sim, no se mostrarán aunque el sketch esté visible.
	• La restricción de colinearidad debe ser entre dos segmentos.
	• La restricción de coincidencia debe permitir unir 2 markers, pero también hacer pertenecer un punto a una recta o un círculo en función de los 2 elementos que se seleccione. De esta forma podemos incluir "OnCircle" como una restricción de coincidencia. coincidencia.
	• Revisa la restricción de concentricidad entre un arco y una circunferencia.
	• La restricción de simetría debe tener como entrada dos markers y una linea o linea infinita que indique el plano de simetría.
	• El Botón Solve no tiene efecto.
